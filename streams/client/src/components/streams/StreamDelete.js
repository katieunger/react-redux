import React from 'react';
import Modal from '../Modal';

const StreamDelete = () => {
    return (
        <div>
            StreamDelete
            <Modal></Modal>
        </div>  
    );
};

export default StreamDelete;